ssl = required # Better: required, or at least 'yes'
ssl_cert = </etc/dovecot/ssl/dovecot.pem  # Path set by script
ssl_key = </etc/dovecot/ssl/dovecot.key    # Path set by script
ssl_min_protocol = TLSv1.2
# ssl_cipher_list = HIGH:!ADH:!NULL # Adjust as needed for security